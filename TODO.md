## TODO

- [ ] Add file and image loading support (`--file`, `--image` options). The Moonshot API is OpenAI-compatible, so multimodal inputs (vision) and file content should work via the standard message format (content array with text + image_url parts).
